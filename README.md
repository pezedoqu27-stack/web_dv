# Портфолио Графического Дизайнера (Дарья Волегова)

Современный, минималистичный сайт-портфолио с эффектом "живой бумаги", созданными на HTML, CSS и JavaScript.

## ✨ Особенности
- **Дизайн**: Минимализм, пастельные тона (розовый, коричневый).
- **Эффекты**:
  - Текстура бумаги (Noise overlay).
  - Смена цвета фона при скролле.
  - Магнитные кнопки и следящий курсор.
  - Анимация конфетти при отправке формы.
- **Адаптивность**: Полная поддержка мобильных устройств.

## 🚀 Как выложить сайт в интернет

### Способ 1: Netlify Drop (Самый простой)
1. Зайдите на [app.netlify.com/drop](https://app.netlify.com/drop).
2. Перетащите папку с сайтом (`Test_web`) в область загрузки.
3. Сайт опубликуется мгновенно! Вы получите ссылку вида `random-name.netlify.app`.
4. В настройках можно изменить имя сайта или подключить свой домен (например, `volegova.design`).

### Способ 2: GitHub Pages (Надежный)
1. Создайте репозиторий на GitHub.
2. Загрузите файлы проекта.
3. Перейдите в **Settings** -> **Pages**.
4. В разделе **Build and deployment** выберите ветку `main` и нажмите **Save**.
5. Сайт будет доступен по адресу `ваше-имя.github.io/репозиторий`.

## 🛠 Как редактировать контент

### Изменение текста и ссылок
Откройте файл `index.html` в любом текстовом редакторе (VS Code, Notepad++).
- **Имя и заголовки**: Ищите текст "Дарья Волегова" и меняйте на свой.
- **Проекты**: Найдите секцию `<div class="portfolio-grid">`. Каждый проект — это блок `<article>`.
  - Измените название в теге `<h3>`.
  - Измените описание в теге `<p>`.

### Изменение фото
1. Поместите свои изображения в папку с сайтом.
2. В `styles.css` или `index.html` замените заглушки на пути к вашим фото.
   - *Совет*: Для лучших результатов используйте оптимизированные JPG или WEBP.

### Изменение цветов
Все цвета настроены в файле `styles.css` в самом начале (секция `:root`).
- `--accent`: Основной акцентный цвет (коричневый).
- `--pink-light`: Цвет фона.

## 📦 Структура файлов
- `index.html` — Основная страница.
- `styles.css` — Стили, цвета, анимации.
- `script.js` — Логика (курсор, фильтры, форма).
